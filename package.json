{
  "name": "radar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AI seed investor for pitch practice",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "pitch": "node --env-file=.env --experimental-strip-types src/cli/pitch.ts",
    "smoke": "node --env-file=.env --experimental-strip-types src/cli/smoke.ts",
    "eval": "node --env-file=.env --experimental-strip-types src/cli/eval.ts",
    "test": "node --experimental-strip-types --test 'src/**/*.test.ts'",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
